//tabuada

let multiplicador = 9
let n = 10 // até quanto vai multiplicar
let i = 1


while (i<=n) {
    let resultado = multiplicador * i
    let texto = `${multiplicador} X ${i} = ${resultado}`
    console.log (texto)
   i++
}

